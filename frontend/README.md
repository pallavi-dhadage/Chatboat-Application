# Frontend (React + Vite)

Dev:

```bash
cd frontend
npm install
npm run dev
```

Environment:
- `VITE_API_BASE` — REST API base (default: http://localhost:5000/api/v1)
- `VITE_SOCKET_URL` — Socket.IO server URL (default: http://localhost:5000)
