# Local dev requirements — runs without Docker, PostgreSQL, Redis, or MySQL
# Uses SQLite for DB, in-memory cache instead of Redis

Flask==2.3.3
Flask-Cors==4.0.0
Flask-SocketIO==5.3.6
Flask-JWT-Extended==4.6.0
Flask-SQLAlchemy==3.1.1
bcrypt==4.1.3
groq==0.9.0
python-dotenv==1.0.1
requests==2.31.0
simple-websocket==1.0.0
